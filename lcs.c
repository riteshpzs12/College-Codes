#include<stdio.h>
#include<stdlib.h>

#define N 1000

int main(int argc, char *argv[]) {
  char s1[N], s2[N];
  scanf("%s%s", s1, s2);
  printf("S1: %s\n", s1);
  printf("S2: %s\n", s2);
  return 0;
}
